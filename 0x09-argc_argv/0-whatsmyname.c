#include <stdio.h>

/**
 * main - Entry point into the program
 * argc - Number of arguments passed to the function
 * argv - The arguements passed to the function
 */

int main(int argc __attribute__((unused)), char*argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
